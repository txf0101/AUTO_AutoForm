# Flexible Script Shared Helpers

This folder is reserved for stable helper files shared by promoted flexible
scripts. First-stage helpers stay inside `autoform_agent/flex_scripts` so they
can be imported, tested, and versioned with the Python package.
